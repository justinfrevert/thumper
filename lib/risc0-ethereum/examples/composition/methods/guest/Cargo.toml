[package]
name = "exponentiate"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "0.21", features = ["std"], default-features = false }

hello-world-methods = { path = "../../../hello-world/methods" }
bytemuck = "1.14"
